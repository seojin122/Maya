# 3D UAM City Simulation – HoverCar & Flying Taxi

미래 도시를 배경으로 **호버카(HoverCar)** 3대와 **드론 택시(Flying Taxi)** 1대를 Autodesk Maya Python 스크립트(`maya.cmds`)로 구현한 절차적 모델링 및 애니메이션 프로젝트입니다.

- **툴**: Autodesk Maya 2025
- **스크립트 언어**: Python (`maya.cmds`)
- **테마**: UAM(도심항공교통), 미래 교통수단, 플라잉카



https://github.com/user-attachments/assets/ae634ea0-8514-4a4a-8d6a-ff28f2722512


---

## 팀 구성 & 역할

### 재희

- HoverCar 모델링 및 재질 설정  
- 엔진/패드 발광 애니메이션  
- UAM 비행 경로 설계 및 키프레임 애니메이션  
- 도시(빌딩/나무/바닥) 환경 구성  

### 서진

- Flying Taxi(드론 택시) 모델링  
  - 바디, 지붕, 앞유리, 헤드라이트, 암, 프로펠러 구조 설계  
- 프로펠러 회전 Expression 구현  
- 드론 택시 비행 경로 애니메이션 설계  


---

## 주요 연출

### 1. HoverCar (by 재희)
> **Script**: `3dfinal2.py`

* **미래형 호버카 3대 생성**
  * 바디, 시트, 등받이, 캐노피(유리), 엔진, 호버 패드 등 디테일한 파츠 구성
  * 반투명 캐노피 + 메탈 재질 + 발광 파츠로 SF 느낌 표현
* **엔진 & 패드 발광(Glow) 애니메이션**
  * `incandescence` 값을 `sin` 함수로 변화시켜 엔진/호버패드가 맥동하듯이 빛나는 효과 구현
* **UAM 비행 경로 애니메이션**
  * 각 호버카마다 서로 다른 경로를 따라 이동
    * **Path A**: 도시 사이를 가로지르며 상승
    * **Path B**: 상공을 곡선으로 비행
    * **Path C**: 더 높은 고도에서 대각선 궤적 이동
  * `translateX/Y/Z` 키프레임 + `spline` 탄젠트로 부드러운 곡선 움직임
* **호버 연기(Hover Motion)**
  * 제자리에서 `Y`축을 살짝 흔들어주는 호버 효과
  * 일부 프레임 보정(`clean_hover_spike`) 및 과장(`exaggerate_hover`) 처리
* **도시 환경**
  * 바닥 Plane, 빌딩, 나무 등 간단한 로우폴리 도시 씬 생성
  * 각 오브젝트에 기본 `lambert` 재질 할당

### 2. Flying Taxi (by 서진)
> **Script**: `taxi.py` 

* **드론 택시 모델링**
  * 직육면체 바디 + 반구형 지붕 + 앞유리로 자동차와 드론의 하이브리드 형태
  * 앞쪽 **헤드라이트** 추가
  * 양쪽에 암(Arm) 4개를 뻗어서 **프로펠러 4개** 장착
* **프로펠러(Propeller) 회전**
  * `polyTorus + polyCube` 조합으로 덕트형 팬 구성
  * **Expression 사용**:
    ```python
    rotor_FL_grp.rotateY = time * 60;
    rotor_FR_grp.rotateY = time * 60;
    rotor_RL_grp.rotateY = time * 60;
    rotor_RR_grp.rotateY = time * 60;
    ```
    → 타임라인에 따라 계속 회전하는 애니메이션 구현
* **비행 애니메이션**
  * **Start**: 화면 왼쪽 아래에서 등장
  * **Middle**: 화면 중앙 상단을 통과
  * **End**: 오른쪽 위로 빠져나가는 연출
  * `translateX/Y/Z`, `rotateY`에 키프레임 후 `spline` 보간으로 부드러운 비행 처리

---


## 사용 기술

### 1) maya.cmds 기반 절차적 모델링

- `polyCube`, `polySphere`, `polyCylinder`, `polyTorus`, `polyPlane` 등 기본 프리미티브 활용  
- 스크립트만으로 모델링, 배치, 스케일 조정, 피벗 정렬까지 자동화

### 2) 키프레임 애니메이션

- `setKeyframe`, `keyTangent`, `scaleKey`, `cutKey` 활용  
- `translateX / Y / Z`, `rotateY`에 키프레임을 찍어 경로 기반 이동 구현  
- `spline` 탄젠트로 자연스럽게 이어지는 곡선 궤적 표현

### 3) 표현식(Expression) 애니메이션

- Maya `expression` 노드를 활용한 **시간 기반 애니메이션**
  - 드론 택시 프로펠러 회전
  - 호버카 엔진/패드 발광 강도 변화
- `time` 값을 직접 사용하여 프레임에 따라 연속적인 모션 생성

### 4) 셰이딩 & 재질

- 사용 재질: `lambert`, `blinn`
- 주요 속성:
  - `color`: 기본 색상
  - `transparency`: 유리/캐노피의 반투명 표현
  - `incandescence`: 엔진/패드 발광 효과

---

## 향후 확장 아이디어

- 카메라 컷 씬 추가  
  - 예: 도시 전경 → 호버카 추적 → 택시 클로즈업 등 시네마틱 연출
- 밤/낮 조명 세팅 및 렌더링 룩 개발 (Arnold 사용)
- UAM 이착륙 스테이션, 도로/하늘 레이어 등 환경 디테일 추가
- Alembic 캐시로 내보낸 뒤, 다른 DCC 툴이나 게임 엔진으로 연동

---

🎓 이 프로젝트는 **미래 교통수단(UAM)** 을  
절차적 모델링 + 애니메이션 연습을 통해 구현한 결과물입니다.  
HoverCar와 Flying Taxi 각각의 스타일과 움직임을 비교해 보면서  
팀원별 아이디어와 연출 차이도 함께 보여주는 데에 의미를 두었습니다.
